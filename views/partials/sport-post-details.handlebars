<section class="card">
  <a href="/api/post/{{id}}">
    <div class="card-body">
      <h3>{{title}}</h3>
      <p>{{format_date date_created}}</p>
    </div>

    <div class="card-body">
      <p id="content">{{content}}</p>
    </div>
  </a>
</section>