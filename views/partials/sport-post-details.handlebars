<section class="card">
  
    <div class="card-body">
      <h2>{{title}}</h2>
      <p>{{date_created}}</p>
    </div>

    <div class="card-body">
      <p>{{content}}</p>
    </div>
    
</section>