{{!-- {{#if loggedIn}} --}}

{{!-- <section class="blog-post">
  <div class="blog-post-details">
    <h3>{{post.title}}</h3>
    <h5>{{post.user_id}}</h5>
    <h5>{{post.date_created}}</h5>
    <p>{{post.content}}</p>
    <button id="add-blog-post-btn">Add New Blog Post</button>
  </div>
</section> --}}

<!-- creating and posting a new blog post -->
<form class="form post-form">
  <div>
    <label for="post">Blog Post</label>
    <input id="post-content" type="text" required />
     <label for="post">Blog Post</label>
    <input id="post-content" type="text" required />
     <label for="post">Blog Post</label>
    {{!-- get rid of required if not required --}}
    <input id="post-content" type="text"/>
  </div>

  <div>
    <button id="submit--new-post" type="submit">Submit</button>
  </div>
</form>


<!-- Ask them to login via login link supplied -->
{{!-- {{else}}<a href="/login">Please log in to view this page</a>
{{/if}} --}}
</section>

{{#each posts as |post|}}
<section class="blog-post">
  <div class="blog-post-details">
{{#if post.looking_for_players}}
<p>Looking for Players</p>
{{/if}}
{{#if post.looking_for_students}}
<p>Looking for Students</p>
{{/if}}
{{#if post.looking_for_coach}}
<p>Looking for Coach</p>
{{/if}}
    <h3>{{post.title}}</h3>
    {{!-- can wrap in a tag to display username of person who wrote post and not id to be able to click into their profile? --}}
    <a href="/profile/{{post.user_id}}">
    <h5>{{post.user.username}}</h5>
    </a>

    <h5>{{post.date_created}}</h5>
    <p>{{post.content}}</p>
    <button id="add-blog-post-btn">Add New Blog Post</button>
  </div>
</section>
{{/each}}


<script src="/js/createblogpost.js"></script>